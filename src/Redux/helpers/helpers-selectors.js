export const isOpenAskModal = ({ helpers }) => helpers.areYouSureModal;
export const deleteProductName = ({ helpers }) => helpers.deleteProduct.name;
export const deleteProductId = ({ helpers }) => helpers.deleteProduct.id;
